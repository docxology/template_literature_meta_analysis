# Fixtures — deep_research

Recorded deep-research report for deterministic, offline CI replay. The bundled
`recorded_report.json` is **synthetic** (example.org citation URLs, reserved
`10.5555/`-style provenance) — it demonstrates the machinery, not real findings.
Do not edit without updating `tests/test_deep_research_adapter.py` and any
manuscript claims that reference it.
