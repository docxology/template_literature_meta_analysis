# `src/visualization/advanced/`

Advanced corpus visualizations built on the analysis outputs: embedding
projections, term/topic heatmaps, dendrograms, and word clouds. Each module is a
pure plotting helper that takes computed arrays/weights and writes a PNG; all
numerical work lives upstream in `src/analysis/`.
