.coverage
.coverage.*
.pytest_cache/
__pycache__/
*.pyc
.benchmarks/

# Egg-info from local installs (any location — guards against renamed-fork drift).
**/*.egg-info/

# Project-local virtualenvs (root .venv is shared)
.venv/

# Provider-downloaded full text is a local retrieval cache, not redistributable
# exemplar evidence. Commit derived assessments and provenance, never raw text.
output/fulltext/
